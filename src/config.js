/**
 * Shared extension configuration.
 *
 * Single source for the local 24/7 bot server URL, imported by both the popup
 * and the background token-sync. Change here when pointing at a hosted server.
 */
export const BOT_URL = 'http://localhost:3000';
